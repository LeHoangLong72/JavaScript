// Toán tử 3 ngôi
let number = -10;
let message = number >= 0 ? "Số dương" : "Số âm";
console.log(message);

let number2 = 10;
let message2 = number2 % 2 === 0 ? "Số chẵn" : "Số lẻ";
console.log(message2);
